# Copy to secret.yaml (gitignored) and set the broker credentials you created
# with mosquitto_passwd. Referenced from configuration.yaml as `!secret <key>`.
mqtt_user: homesync
mqtt_password: CHANGE_ME
